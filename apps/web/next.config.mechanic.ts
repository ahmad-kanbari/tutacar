import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Separate build directory for mechanic dashboard
  distDir: '.next-mechanic',
};

export default nextConfig;
